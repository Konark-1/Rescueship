# assets/

> **Purpose**: Static SVG branding assets bundled by Vite into the frontend build.

## File Catalog

| File | Role |
|------|------|
| `react.svg` | React logo (used in dev demo remnants) |
| `vite.svg` | Vite logo with radial gradient filters |

These are legacy Vite scaffold assets. Application icons live in `../components/icons/`.
